# Required for SSR API fetching in production (set to your deployed URL)
# For local dev, defaults to http://localhost:3000 — no configuration needed
NEXT_PUBLIC_BASE_URL=https://your-deployed-url.vercel.app
